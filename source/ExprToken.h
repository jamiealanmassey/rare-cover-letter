#ifndef _EXPR_TOKEN_H
#define _EXPR_TOKEN_H

/** Descriptive tokens giving meaning to each word in the input **/
enum ExprToken
{
	EXAMINE = 0,
	MOVE = 1,
	DESCRIBE = 2,
	EXIT = 3,
	HELP = 4,
	WORD = 5,
};

#endif // _EXPR_TOKEN_H
